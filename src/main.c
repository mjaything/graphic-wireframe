#include "fdf.h"

int main(int argc, char **argv)
{
    int fd;
    // t_map    *map;
    t_env   *env;

    if (argc != 2)
    {
        // "Invalid argument";
        return (0);
    }
    if ((fd = open(argv[1], O_RDONLY)) < 0)
        r
    map = map_init();
    if 
}
